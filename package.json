{
  "name": "kultur-yolu-capacitor",
  "version": "1.2.1",
  "private": true,
  "description": "Kültür Yolu quiz — offline vanilla JS; white text; APK name update.",
  "scripts": {
    "cap:add:android": "npx cap add android",
    "cap:sync": "npx cap sync",
    "android": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.0",
    "@capacitor/android": "^6.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.0"
  }
}